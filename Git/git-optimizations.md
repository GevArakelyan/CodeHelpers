## Git lfs makes working with large files faster

git lfs install
git lfs track "images/*"
or
git lfs track "*.png"

git add .gitattributes
git commit -m "Add git-lfs"


### To see which files are lfs-files
git lfs ls-files


